<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <h3>Item</h3>
    <% for(var i=0; i < items.length; i++) { %>
      <p>
        <span><%= items[i].id %></span>
        <span><%= items[i].name %></span>
        <span><%= items[i].number %></span>
      </p>
   <% } %>
    <!-- <p><%= items %></p> -->
  </body>
</html>
